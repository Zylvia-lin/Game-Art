# DESIGN.md

## 项目与用户画像
- AI 游戏美术资产生成平台，面向独立游戏开发者和游戏美术设计师
- 用户需要高效、直观的创作工具，不需要复杂的节点连线操作

## 品牌与视觉方向
- 深色主题为主，专业创作工具感
- 灵感来源：Figma / Linear / Vercel Dashboard 的暗色调设计语言
- 整体气质：专业、克制、高效，像一个高端的创作工作台

## Design Tokens

### 色彩
- 背景主色：深灰近黑 `#0a0a0f`，营造沉浸式创作氛围
- 侧边栏：`#111118`，与主区域形成微妙层次
- 卡片/面板：`#16161f`，带细微边框
- 主强调色：紫蓝渐变 `#6366f1` → `#8b5cf6`，用于按钮、高亮、进度指示
- 文字主色：`#e4e4e7`（zinc-200），次要文字 `#a1a1aa`（zinc-400）
- 成功色：`#22c55e`，警告色：`#f59e0b`，错误色：`#ef4444`
- 边框色：`#27272a`（zinc-800），hover 时 `#3f3f46`

### 字体
- 主字体：Inter（英文）/ PingFang SC（中文）
- 等宽字体：JetBrains Mono，用于提示词编辑和代码展示
- 标题：font-semibold，正文：font-normal
- 层级：页面标题 24px，卡片标题 16px，正文 14px，辅助文字 12px

### 圆角
- 按钮/输入框：8px
- 卡片：12px
- 弹窗/大面板：16px

### 阴影
- 卡片：`0 0 0 1px rgba(255,255,255,0.05)` + `0 4px 24px rgba(0,0,0,0.3)`
- 悬浮提升：`0 8px 32px rgba(0,0,0,0.4)`

### 动效
- 过渡：`transition-all duration-200 ease-out`
- 按钮 hover：微上移 `translateY(-1px)` + 边框高亮
- 页面切换：淡入 `opacity 0→1` + 微上移

## 布局与响应式
- 侧边栏固定宽度 240px，可折叠至 60px
- 主内容区自适应
- 工具页面三栏布局：左参数面板 320px | 中间画布自适应 | 右侧历史 280px
- 最小宽度 1280px（桌面优先的创作工具）

## 交互与状态
- 生成中：骨架屏 + 进度条动画
- 图片加载：渐显 + 模糊占位
- 空状态：居中图标 + 引导文案
- 错误状态：红色边框 + 错误消息 toast

## 设计禁忌
- 不要使用亮色/白色背景
- 不要使用过多颜色，保持克制
- 不要使用圆角过大的元素（最大 16px）
- 不要使用花哨的渐变背景
- 不要使用 emoji 作为图标（使用 Lucide Icons）
